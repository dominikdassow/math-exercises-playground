const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      fontFamily: {
        rubik: ['"Rubik"', "sans-serif"]
      }
    }
  },

  plugins: []
}

module.exports = config
